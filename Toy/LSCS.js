const LSCS = function (arr) {
  //일단 양수면 무조건 더함
  // 음수이면 그 뒤에 음수가 나올때 까지의 합보다 작으면 더함
  const result = []
  arr.forEach((num, idx, origin) => {
    num
  })
};
LSCS([1, 2, 3, -4, 5, -4, 5, -4, -4, -1]) // 8